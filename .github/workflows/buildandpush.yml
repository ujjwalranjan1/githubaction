name: test

on:
  push:
    branches:
      - 'main'

jobs:
  dockerpullanddeploy:
    runs-on: ubuntu-latest
    steps:
      -
        name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ayushgoel1
          password: dckr_pat_C_S-xNXGZH6KQbGzD54_zd7GVE4
      -
        name: pull
        run: docker pull ayushgoel1/test1
      -
        name: createcontainer
        run: docker run -t testcontainer -d ayushgoel1/test1 
      - 
        name: execute
        run: docker exec -it testcontainer c++ a.cpp
      
